{% extends 'admin.html.twig' %}

{% block title %}Créer une Todolist{% endblock %}

{% block body %} {{ parent() }}

 <!-- CREATE Task form  -->
<div> 
    <h4> Mes Nouvelles Tâches </h4>
</div>

<div> 
     {{ form_start(form) }}  
        {{ form_row(form.name) }}
        {{ form_row(form.color) }}
        <button type="submit" class="btn btn-primary">Valider</button>
        <a href="{{ path('admin') }}"><i class="fas fa-hand-point-left"></i>Retour</a>
    {{ form_end(form) }}
</div>

{% endblock %}
